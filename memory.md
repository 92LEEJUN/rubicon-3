# 협업 / 실행 패턴 (Memory)

> 세션을 거치며 쌓인 **실행 노하우**. 규칙(`CLAUDE.md`)이 "무엇을 따르나"라면, 여기는
> "어떻게 효율적으로·안전하게 진행하나"다. 큰 변경·다계층 작업에서 특히 참고한다.

## 병렬 subagent — disjoint 소유

큰 변경은 **파일/디렉토리를 겹치지 않게 나눠** subagent로 병렬 처리할 수 있다.

- **소유 분리**: 각 에이전트가 **자기 파일/디렉토리만** 편집(예: `bff/` ↔ `backend/app/api/internal.py` ↔ `frontend/`). 겹치면 충돌.
- **git은 메인이 통합**: 에이전트는 **git 금지**(commit/push 안 함)·**자기 테스트만** 실행. 메인이 전체 스위트 + 통합 커밋.
- **교차 계약은 공유 블록**: 에이전트 간에 맞춰야 할 계약(신원 헤더·필드명·상태코드 등)은 **동일한 "공유 계약 블록"을 모든 프롬프트에 그대로** 삽입한다.
- **의존 인터페이스 동결**: 다른 에이전트 파일이 의존하는 시그니처(생성자·함수 인자·`build_container()` 등)는 **변경 금지로 못박는다.** 바꿔야 하면 그 파일 소유자에게 몰아준다.

## 통합 리뷰가 inter-agent 드리프트를 잡는다

각 에이전트가 **내부적으로 일관**해도 **계층 경계에서 어긋난다.**
- 실제 사례: FE가 `/orders/commit`을 호출했는데 BFF 실제 라우트는 `/orders` → 통합 전엔 양쪽 테스트 다 green이지만 실제론 404.
- 그래서 메인은 통합 시 **경계 계약을 스폿체크**한다: 경로·필드명(예: `part_ids` vs `product_ids`)·상태코드(401/409)·헤더 이름.

## 체크포인트 커밋 타이밍

- 백그라운드 에이전트가 작업 중일 때 **`git add -A` 금지** — 다른 에이전트의 WIP를 캡처한다.
- **특정 경로만** add 해서 커밋(예: `git add docs/adr/0050-*.md`), 통합 커밋은 에이전트 완료 후.
- 원자적 파일 쓰기라 import는 깨지지 않지만, 논리적 반쪽 상태는 가능 → 최종 검증은 메인이 전체 스위트로.

## 검증 자산

- **실 LLM은 CI에 없다 → 수동.** `backend/verify_*.py`:
  - `verify_llm_planner.py` — 규칙 vs 실 LLM 라우팅 plan.
  - `verify_multiturn_long.py` — 장문 멀티턴 결정적 출력.
  - `verify_e2e_timing.py` — 패리티 + 구간/총 레이턴시(E2E ≈ LLM 홉 1회).
- **정본 테스트셋**: `specs/capability-orchestrator/test-set.md`(케이스ID↔발화↔기대 plan↔검증 위치).
- 회귀 자동화는 stub 플래너로 결정적 고정(`backend/tests/test_multiturn_long.py`); 실 LLM 정확도는 위 하니스로 수동.

## 정직성 / 범위

- 큰 작업을 "전부 진행"하라고 받아도, **풀 구현 vs 안전 증분**을 구분해 보고한다(예: DB 영속은 3 리포만, order는 후속). 회귀를 깰 위험이 크면 멈추고 사유를 남긴다.
- 삭제는 신중히: 옛 경로(legacy/runtime)는 **대체물이 준비된 뒤** 제거. core는 capability로 수렴 후 제거했지만 legacy/runtime(LLM prose)은 §8~11 전까지 유지.
