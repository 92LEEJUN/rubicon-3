# bff/.env 로 복사해 값을 채운다. .env 는 gitignore 된다.
#   cp .env.example .env
#
# BFF → BE 내부 API 베이스 URL. BFF의 BackendClient가 이 주소로 BE를 호출한다.
# 기본값은 로컬 개발(BE가 8001 포트)에 맞춘다.
BE_BASE_URL=http://localhost:8001
