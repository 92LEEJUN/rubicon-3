# backend/.env 로 복사해 실제 값을 채운다. .env 는 gitignore 되어 커밋되지 않는다.
#   cp .env.example .env

# OpenAI API 키 (필수 — LLM 경로 사용 시). 저장소/코드에 키를 직접 넣지 말 것.
OPENAI_API_KEY=

# LLM 모델 (기본 gpt-4o-mini)
LLM_MODEL=gpt-4o-mini

# 1 = 채팅 응답을 LLM 자연어 생성(tool-loop). 비우면 결정적(룰베이스+Mock) 경로.
LLM_BACKED=
