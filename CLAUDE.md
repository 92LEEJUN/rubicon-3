# 작업 규칙 (Work Rules)

이 레포는 **스펙 기반(spec-driven) 워크플로우**를 따른다.
**큰 기능 단위의 작업**은 코드를 작성하기 전에 아래 3종 문서를 먼저 만들어
틀을 잡고, 단계적으로 진행한다.

> 이 문서는 에이전트(Claude)가 작업마다 따르는 규칙이다.
> 사람을 위한 설명은 `docs/WORKFLOW.md` 를 참고한다.

## 적용 범위

이 워크플로우는 **큰 기능 / 규모 있는 작업**의 틀을 미리 잡기 위한 것이다.
모든 변경에 강제하지 않는다.

**적용한다 (스펙 3종 작성):**
- 새 기능 추가, 큰 기능 단위의 작업
- 여러 컴포넌트/파일에 걸친 변경
- 설계 결정이 필요하거나 영향 범위가 넓은 작업
- 데이터 모델·공개 인터페이스가 바뀌는 작업

**적용하지 않는다 (바로 진행):**
- 오타·문구 수정, 사소한 버그 픽스
- 설정값·상수 변경, 의존성 버전 업데이트
- 포맷팅·리네이밍 등 동작이 바뀌지 않는 변경
- 한 파일 안에서 끝나는 작은 변경

> 애매하면 "이게 큰 기능인가?"를 기준으로 판단하고, 그래도 모호하면
> 사용자에게 스펙을 만들지 물어본다.

## 문서 계층 (기반 문서 vs 스펙 문서)

문서는 두 계층으로 나눈다.

**1) 기반 문서 (공유, 상위) — `docs/`**
- `docs/architecture.md` — 전체 시스템 아키텍처, 기술 스택, Mock↔실 전략 등 제품·시스템 수준 설계.
- `docs/data-model.md` — 여러 스펙이 공유하는 도메인 데이터 모델·스키마·클래스 구조, Repository/Port 인터페이스 타입.
- `docs/response-templates.md` — FE↔BE 공유 응답 표현 계약(템플릿 종류·스키마·선택/CTA 규칙).
- `docs/frontend-architecture.md` — React Native 앱 내부 구조·결정(상태관리·네비·트랜스포트 등).
- `docs/wireframes.md` — 저충실도 화면 명세(레이아웃·흐름). 비주얼 디자인 아님.
- `docs/api-contract.md` — 클라이언트↔서버 외부 노출 인터페이스(WS `/chat`·도메인 엔드포인트), API/DB 경계.
- `docs/orchestration.md` — LLM 오케스트레이터 내부(의도 분류·tool·RAG·스트리밍·멀티에이전트 다단계).
- `docs/agents.md` — 멀티에이전트 구조(슈퍼바이저-워커·에이전트/tool 구성·조건부 리뷰·통신/병렬).
- `docs/operations.md` — 멀티유저 운영·스케일(동시성·병목·비용 캐싱·세션/상태 격리·개인화 컨텍스트 조립).
- `docs/llm-policy.md` — LLM 프롬프트 정책(해야 할/하지 말아야 할 말·어투·가드레일·개인화 주입).
- `docs/analytics.md` — 사용 분석 이벤트 택소노미(퍼널·전환 기여·체류), FE↔BE 공유 계약.
- `docs/adr/` — **결정 기록(ADR)**. 위 기반 문서가 결정의 *결과*라면, ADR은 결정의 *이유·후보안·기각 이유*. 번호순. 인덱스 `docs/adr/README.md`.
- 이들은 **여러 스펙이 공유하는 진실의 출처(single source of truth)** 다.

**2) 스펙 문서 (기능별, 하위) — `specs/<작업명>/`**
- 각 기능의 `requirements.md` / `design.md` / `tasks.md`.
- 스펙의 design은 기반 문서를 **참조(링크)** 하고, **기능 고유의 설계만** 담는다.
  공유 데이터 모델·아키텍처를 스펙 design에서 중복 정의하지 않는다.

**규칙:**
- 데이터 모델·공개 인터페이스·전체 아키텍처가 바뀌면, 스펙 design이 아니라
  **기반 문서(`docs/`)를 갱신**한다. 스펙 design은 그 변경을 참조한다.
- 새 스펙을 시작할 때 관련 기반 문서를 먼저 읽고, 그 모델·인터페이스 위에서 설계한다.

## 핵심 원칙

1. **문서 먼저, 코드 나중.** (적용 범위에 해당하는 작업에 한해) 코드를 작성하기 전에
   requirements → design → tasks 순서로 문서를 만든다.
2. **단계별 진행.** 각 단계는 이전 단계가 끝난 뒤에 시작한다.
   앞 단계를 건너뛰지 않는다.
3. **추적 가능성.** design은 requirements를, tasks는 requirements/design을
   명시적으로 참조한다.

## 작업 절차

### 0. 작업 폴더 생성
- 작업마다 `specs/<작업명>/` 폴더를 만든다.
- `<작업명>` 은 케밥케이스(kebab-case)로 간결하게 짓는다.
  예: `specs/user-login/`, `specs/fix-csv-parsing/`
- 폴더 안에 `requirements.md`, `design.md`, `tasks.md` 3개 파일을 둔다.
- 템플릿은 `specs/_template/` 에 있다. 복사해서 사용한다.

### 1. requirements.md (요구사항)
- **무엇을 / 왜** 를 정의한다. 구현 방법은 적지 않는다.
- 각 요구사항은 **User Story + 수용기준(Acceptance Criteria)** 형식으로 쓴다.
  - User Story: `~로서, ~를 원한다, 그래서 ~할 수 있다`
  - 수용기준: **EARS** 형식의 검증 가능한 문장
    (`~할 때, 시스템은 ~해야 한다 (SHALL)`)
- 모든 요구사항에 고유 번호를 매긴다. (요구사항 1, 2, ...)

### 2. design.md (설계)
- requirements를 **어떻게** 만족시킬지 설계한다.
- 최소한 다음을 포함한다: 개요, 아키텍처, 주요 컴포넌트/인터페이스,
  데이터 모델, 에러 처리, 테스트 전략.
- 각 설계 결정이 어떤 요구사항을 충족하는지 연결한다.

### 3. tasks.md (작업 수행)
- design을 실제 구현으로 나눈 **체크리스트**를 만든다.
- 각 항목은 작고 검증 가능한 단위로 쪼갠다.
- 각 항목 끝에 관련 요구사항 번호를 표기한다. 예: `(요구사항 1, 3)`
- 작업을 진행하며 완료된 항목은 `[x]` 로 체크한다.

## 진행 규칙

- requirements/design 단계에서 모호하거나 결정이 필요한 부분이 있으면
  **추측하지 말고 사용자에게 질문**한다.
- 각 단계 완료 후 다음 단계로 넘어가기 전에 사용자 확인을 받는 것을 기본으로 한다.
- 구현 중 설계와 달라지는 부분이 생기면 코드만 바꾸지 말고
  해당 문서(design/tasks)도 함께 갱신한다.

## 브랜치 / PR 규칙

- 브랜치명은 **`<유형>/<상세>`** 형태로 짓는다.
  - **유형** — 작업 목적: `feat`(기능), `fix`(버그), `doc`(문서), `cicd`(CI/CD).
    필요 시 `refactor`·`test`·`chore` 등을 추가로 쓴다.
  - **상세** — 작업 내용을 케밥케이스(kebab-case)로 간결하게.
  - 예: `feat/chat-streaming`, `fix/csv-parsing`, `doc/branch-naming`, `cicd/build-pipeline`.
- 작업은 지정 브랜치에서 진행하고, 완료 후 PR을 만든다.

## 문서 언어
- 모든 스펙 문서와 규칙은 **한국어**로 작성한다.
